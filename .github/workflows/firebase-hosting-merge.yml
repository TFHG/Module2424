# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge

on:
  push:
    branches:
      - Y  # Ensure this is the branch you use for live deployment

jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      # Checkout the repository
      - uses: actions/checkout@v4
      
      # Install npm dependencies
      - name: Install Dependencies
        run: npm ci

      # Remove or comment out the build step since it's not required for your Express app
      # If needed, add a simple build step placeholder:
      # - name: Build Project
      #   run: echo "No build step required"

      # Deploy to Firebase Hosting
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_MODULE2424CICD }}
          channelId: live
          projectId: module2424cicd
